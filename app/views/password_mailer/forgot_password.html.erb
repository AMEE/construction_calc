Dear <%=h !@user.name.empty? ? @user.name : @user.login %>,

We recently received a password reset for your account (with username '<%= @user.login %>'). You can change your password at this URL: <%= @url %>

For your safety this link will be valid for 2 weeks, after which you will need to re-request a password reset