<tr><td><%= delivery.name %></td><td><%= delivery.amee_category.name %></td><td><%= delivery.amount %> <%= units_name_from_amee_unit(delivery) %></td><td><%= delivery.carbon_output_cache %></td><td class="middle"><%= link_to_remote(image_tag("delivery-edit.png", :style => "margin:0;padding:0;"), :url => edit_project_delivery_path(delivery.project, delivery), :method => :get) %></td><td clas="middle"><%= button_to("delete", project_delivery_path(delivery.project, delivery), :method => :delete, :confirm => "Are you sure?", :class => "delete-button") %></td></tr>