<tr><td><%= commute.id %></td><td><%= link_to(commute.name, edit_project_commute_path(commute.project, commute)) %></td><td><%= button_to("delete", project_commute_path(commute.project, commute), :method => :delete, :confirm => "Are you sure?") %></td></tr>