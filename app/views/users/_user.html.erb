<tr><td><%= link_to(user.id, user_roles_path(user)) %></td><td><%= link_to(user.name, user_roles_path(user)) %></td><td><%= user.email %></td><td><%= link_to("delete user", user_path(user), :method => :delete, :confirm => "Are you sure you want to delete this user?") if current_user.admin? || current_user.client_admin?(@client) %></td></tr>