<tr><td><%= user.login %></td><td><%= user.name %></td><td><%= user.email %></td><td class="middle"><%= link_to(image_tag("user-edit.png", :style => "margin:0;padding:0;"), user_roles_path(user)) %></td><td class="middle"><%= link_to("delete user", user_path(user), :method => :delete, :confirm => "Are you sure you want to delete this user?", :class => "delete-button") if current_user != user && (current_user.admin? || current_user.client_admin?(@client)) %></td></tr>